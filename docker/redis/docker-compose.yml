version: '3'
services:
  redis:
    image: redis
    container_name: redis
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
volumes:
  redis-data:
networks:
  default:
    external:
      name: docker-network