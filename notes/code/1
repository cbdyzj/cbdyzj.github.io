// js sleep
const sleep = ms => new Promise(resolve => setTimeout(resolve, ms))

// js 父集含子集
const setContains = (set, subset) => subset.every(e => set.includes(e))

# py 父集含子集
setContains = lambda a, b: [i for i in b if i not in a] == []
